---
import Layout from '../../layouts/Layout.astro';
import CTABanner from '../../components/sections/CTABanner.astro';
import { allParts } from '../../data/parts';

const phone = "(475) XXX-XXXX";
const phoneHref = "tel:+14750000000";

export function getStaticPaths() {
  return allParts.map(part => ({
    params: { slug: part.slug },
    props: { part },
  }));
}

const { part } = Astro.props;

const relatedParts = part.related
  .map((slug: string) => allParts.find(p => p.slug === slug))
  .filter(Boolean)
  .filter((p: any, i: number, arr: any[]) => arr.findIndex((x: any) => x.slug === p.slug) === i)
  .slice(0, 3);
---

<Layout
  title={`${part.fullName} | JP Tadduni Transmissions, Stratford CT`}
  description={`${part.fullName} — ${part.desc.slice(0, 140)}… Call JP Tadduni in Stratford, CT.`}
>

  <section class="bg-jp-bg pt-10 pb-14">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">

      <nav class="flex items-center gap-2 text-xs text-jp-muted mb-6 font-body uppercase tracking-widest flex-wrap">
        <a href="/" class="hover:text-jp-red transition-colors">Home</a>
        <span>/</span>
        <a href="/parts" class="hover:text-jp-red transition-colors">Parts</a>
        <span>/</span>
        <span class="text-jp-charcoal font-semibold">{part.name}</span>
      </nav>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">

        <!-- Image -->
        <div class="rounded-card overflow-hidden shadow-card">
          <img
            src={part.img}
            alt={part.imgAlt}
            class="w-full h-80 lg:h-[480px] object-cover"
            loading="eager"
            decoding="async"
          />
        </div>

        <!-- Info -->
        <div class="flex flex-col gap-6">
          <div>
            <span class="inline-block text-xs font-semibold uppercase tracking-wider text-jp-red bg-red-50 px-3 py-1 rounded-full mb-3">{part.category}</span>
            <p class="font-body text-sm text-jp-muted uppercase tracking-widest mb-1">{part.categoryGroup}</p>
            <h1 class="font-heading font-bold text-3xl sm:text-4xl text-jp-charcoal leading-tight">{part.fullName}</h1>
          </div>

          <p class="font-body text-jp-muted leading-relaxed">{part.desc}</p>

          <div>
            <p class="font-heading font-semibold text-jp-charcoal text-sm uppercase tracking-wider mb-3 border-l-2 border-jp-red pl-3">Specs & Details</p>
            <ul class="flex flex-col gap-2">
              {part.specs.map((spec: string) => (
                <li class="flex items-start gap-3 font-body text-sm text-jp-text">
                  <svg class="w-4 h-4 text-jp-red flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                  </svg>
                  {spec}
                </li>
              ))}
            </ul>
          </div>

          <div>
            <p class="font-heading font-semibold text-jp-charcoal text-sm uppercase tracking-wider mb-3 border-l-2 border-jp-red pl-3">Compatible Vehicles</p>
            <ul class="flex flex-col gap-1.5">
              {part.compatible.map((vehicle: string) => (
                <li class="flex items-center gap-2 font-body text-sm text-jp-muted">
                  <span class="w-1.5 h-1.5 rounded-full bg-jp-red flex-shrink-0"></span>
                  {vehicle}
                </li>
              ))}
            </ul>
          </div>

          <div class="flex flex-col sm:flex-row gap-3 pt-2 border-t border-jp-border">
            <a href={phoneHref} class="btn-primary flex-1 justify-center">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z"/>
              </svg>
              Call to Inquire: {phone}
            </a>
            <a href="/contact" class="btn-secondary flex-1 justify-center">Send a Message →</a>
          </div>

          <p class="font-body text-xs text-jp-muted">Pricing and availability vary. Call or message us and we'll get back to you same day.</p>
        </div>
      </div>
    </div>
  </section>

  {relatedParts.length > 0 && (
    <section class="py-14 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6">
        <h2 class="font-heading font-bold text-2xl text-jp-charcoal mb-8 border-l-4 border-jp-red pl-4">Related Parts</h2>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
          {relatedParts.map((rp: any) => (
            <a href={`/parts/${rp.slug}`} class="parts-card flex flex-col gap-3 group">
              <div class="rounded-md overflow-hidden">
                <img src={rp.img} alt={rp.imgAlt} class="w-full h-44 object-cover group-hover:scale-105 transition-transform duration-300" loading="lazy" decoding="async" />
              </div>
              <span class="inline-block text-xs font-semibold uppercase tracking-wider text-jp-red bg-red-50 px-2 py-1 rounded self-start">{rp.category}</span>
              <h3 class="font-heading font-bold text-base text-jp-charcoal group-hover:text-jp-red transition-colors">{rp.name}</h3>
              <span class="font-body text-sm text-jp-red font-semibold mt-auto">View Part →</span>
            </a>
          ))}
        </div>
      </div>
    </section>
  )}

  <CTABanner />
</Layout>
