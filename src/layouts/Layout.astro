---
/**
 * Layout.astro – Root layout for JP Tadduni Transmissions & Driveline
 * Dark motorsport aesthetic with racing red accents.
 */

import SEOHead from '../components/layout/SEOHead.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  ogImage?: string;
  schema?: string;
}

const { title, description, canonical, ogImage, schema } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <SEOHead
    title={title}
    description={description}
    canonical={canonical}
    ogImage={ogImage}
    schema={schema}
  />
  <body class="min-h-screen flex flex-col bg-jp-black text-jp-gray">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
